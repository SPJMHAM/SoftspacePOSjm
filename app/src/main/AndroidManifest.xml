<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.softspaceposjm">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestChgStatus"></activity>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Req_Service.status">

        </activity>
        <activity android:name=".Registration" />
        <activity android:name=".services.service2_Activity" />
        <activity android:name=".services.serviceInfo_Activity" />
        <activity android:name=".Midguard_customer_dashboard" />
        <activity android:name=".services.Services_Activity" />
        <activity android:name=".TestM1" />
        <activity android:name=".Details" />
        <activity android:name=".confirmation" />
        <activity android:name=".requestSuccess" />
        <activity android:name=".bookingSuccess" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsTest1"
            android:label="@string/title_activity_maps_test1" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".ShowMap" />
        <activity android:name=".security_guard_info1" />
        <activity android:name=".sea_services" />
        <activity android:name=".user_dashboard" />
        <activity android:name=".login_page1" />
        <activity android:name=".user_signup"></activity>
        <activity android:name=".PermissionAccess" />
        <activity android:name=".FingerprintAccess" />
        <activity android:name=".RegistrationComplete"></activity>
        <activity android:name=".DetailsPage1" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>